## Getting Started 

**Using `uv`**

Install dependencies:

```bash
uv sync
```

Start server:

```bash
uv run main.py
```

**Using `pip`**

Install dependencies:

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

Start server:

```bash
python main.py
```